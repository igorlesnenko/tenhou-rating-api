machine:
  services:
      - docker
  machine:
    node:
      version: 6.2.2

test:
  override:
    # no tests yet
    - echo 1

dependencies:
  override:
    - npm install
    - sh build.sh

deployment:
  production:
    branch: stable
    commands:
      - ./deploy_prod.sh
